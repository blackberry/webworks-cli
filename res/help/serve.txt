usage: webworks [--verbose] serve [port]

description:
   Start a webserver to host project assets.

options:
   port                          TCP port to open server on

example:
   $ webworks serve
